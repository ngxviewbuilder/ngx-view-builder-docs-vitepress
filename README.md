# NGX View Builder documentation

VitePress documentation for creators and developers working with NGX View Builder.

```bash
npm install
npm run docs:dev
```

Create a production build with `npm run docs:build`.
